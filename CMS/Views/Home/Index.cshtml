@model ContactView

@{
    ViewData["isHome"] = true;
    ViewData["canonical"] = "https://armatys.me";
}

@await Html.PartialAsync("User/_Slider")
@await Html.PartialAsync("User/_Services")
@await Html.PartialAsync("User/_About")
@await Html.PartialAsync("User/_Opinion")
@await Html.PartialAsync("User/_Portfolio")
@await Html.PartialAsync("User/_CTA")
@await Html.PartialAsync("User/_Blog")
@await Html.PartialAsync("User/_Map")
@await Html.PartialAsync("User/_Contact")


@section homeScripts {
    <script>
        // Tutaj piszemy skrypty dla głównej strony

        // Jeżeli walidacja będzie błędna to przekieruj do sekcji kontaktu
        if (@((Model != null).ToString().ToLower())) {
            window.location.href = "#form";
        }

    </script>
}