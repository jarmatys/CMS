@model TagView

@{
    ViewData["Title"] = "dodawanie tagu";
    Layout = "~/Views/Shared/Admin/_Layout.cshtml";
}
<div class="page-inner">

    <div class="page-header">
        <h4 class="page-title">Dodawnie tagu</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a href="@Url.Action("Index","Admin")">
                    <i class="flaticon-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("List","Article")">Wpisy</a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("List","Tag")">Tagi</a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                Dodaj
            </li>
        </ul>
    </div>

    <div class="card">
        @using (Html.BeginForm("Add", "Tag", FormMethod.Post))
        {
            <div class="card-header">
                <div class="d-flex align-items-center">
                    <h4 class="card-title">Dodaj Tag</h4>
                </div>
            </div>
            <div class="card-body">

                @Html.ValidationSummary(false, "", new { @class = "text-danger" })

                <div class="input-icon mb-3">
                    <span class="input-icon-addon">
                        <i class="fas fa-pen-alt"></i>
                    </span>
                    @Html.TextBoxFor(x => x.Name, new { @class = "form-control", @placeholder = "Nazwa tagu" })
                </div>

                <div class="input-icon mb-3">
                    <span class="input-icon-addon">
                        <i class="fas fa-link"></i>
                    </span>
                    @Html.TextBoxFor(x => x.ShortName, new { @class = "form-control", @placeholder = "Uproszczona nazwa" })

                </div>

                <label for="comment">Opis</label>
                @Html.TextAreaFor(x => x.Description, new { @class = "form-control", @placeholder = "Opis tagu (opcjonalnie)", @id = "comment", @rows = "5" })

            </div>

            <div class="row justify-content-center">
                <button class="btn btn-primary mb-3" type="submit">
                    <span class="btn-label">
                        <i class="fa fa-plus"></i>
                    </span>
                    Dodaj tag
                </button>
            </div>
        }
    </div>
</div>