@{
    ViewBag.Title = "Strona główna";
}

<!DOCTYPE html>
<html lang="en">
<head>
    @Html.Partial("_Head")
</head>
<body>
    @Html.Partial("_Navigation")

    <div>
        @RenderBody()
    </div>

    @Html.Partial("_Footer")
    @Html.Partial("_Scripts")
</body>
</html>